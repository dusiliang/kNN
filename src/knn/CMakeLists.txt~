set (LIBRARY_OUTPUT_PATH ${IMAGE_RECOGNIZOR_SOURCE_DIR}/lib)
set (SRC_LIST *.cpp)

set (CMAKE_C_COMPILER c++)

INCLUDE_DIRECTORIES (${IMAGE_RECOGNIZOR_SOURCE_DIR}/mylib)

add_library(knn STATIC ${SRC_LIST})